import React from "react";
import { View } from "react-native";

// Minimal placeholder - this just needs to exist for the tab button to render
export default function AddRecipeTab() {
  return <View />;
}
