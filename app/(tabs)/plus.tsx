import React, { useEffect } from "react";
import { Redirect } from "expo-router";

export default function PlusScreen() {
  // This is a placeholder file to prevent the "No route named plus" error
  // The actual plus functionality is handled by the PlusMenu component
  return <Redirect href="/(tabs)" />;
}
